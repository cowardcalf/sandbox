const SQUARE_SIZE = 20;

const DIRECTIONS = {
  UP: "ArrowUp",
  DOWN: "ArrowDown",
  LEFT: "ArrowLeft",
  RIGHT: "ArrowRight",
};

const DIRECTION_VALUES = Object.values(DIRECTIONS);

export { SQUARE_SIZE, DIRECTIONS, DIRECTION_VALUES };
